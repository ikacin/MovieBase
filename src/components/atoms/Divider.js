import { Divider } from '@mantine/core';

const CustomDivider = ({color}) => {
    return (
        <>
            <Divider color={color}/>
        </>
    );
}

export default CustomDivider